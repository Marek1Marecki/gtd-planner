"""Django app configuration for areas."""

from django.apps import AppConfig


class AreasConfig(AppConfig):
    """Django configuration for the areas app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.areas"
    label = "areas"
