"""Django app configuration for contexts."""

from django.apps import AppConfig


class ContextsConfig(AppConfig):
    """Django configuration for contexts app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.contexts"
    label = "contexts"
